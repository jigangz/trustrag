# Phase 1 Streaming UI Screenshots

Screenshots to capture during E2E verification (P1-GATE):

1. **idle.png** — Initial state: empty query panel, connected status
2. **streaming.png** — During stream: tokens appearing + status indicator showing "Generating..."
3. **cancelled.png** — Cancel clicked mid-stream: partial answer visible + "Query cancelled" notice
4. **done.png** — Full success: answer + trust badge + sources displayed
5. **error.png** — Error state: Groq rate limit error displayed with code

These screenshots will be captured during the Phase 1 GATE verification step.
